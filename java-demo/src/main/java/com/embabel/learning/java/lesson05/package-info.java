/**
 * Lesson 05 — Human-in-the-loop with {@code WaitFor}.
 * <p>
 * Actions can pause the process and request a typed form submission from the user.
 * High {@code cost} makes HITL a fallback path when cheaper extraction fails.
 */
package com.embabel.learning.java.lesson05;
